"use client";
import { FormEvent,useState } from "react";
import { useRouter } from "next/navigation";
export default function LoginForm(){const [password,setPassword]=useState("");const [error,setError]=useState("");const [loading,setLoading]=useState(false);const router=useRouter();async function submit(e:FormEvent){e.preventDefault();setLoading(true);setError("");const r=await fetch("/api/auth/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({password})});setLoading(false);if(!r.ok){setError((await r.json()).error||"Erro");return}router.push("/admin");router.refresh()}return <div className="loginWrap"><form className="loginCard" onSubmit={submit}><div className="brandLogo"><span className="brandMark" aria-hidden="true"><i></i><i></i><i></i></span><span className="brandWords"><b>Viralizou</b><strong>goiania</strong></span></div><h1>Painel editorial</h1><p>Acesse para publicar e gerenciar as notícias de Goiânia.</p>{error&&<div className="notice error">{error}</div>}<div className="field"><label>Senha</label><input type="password" value={password} onChange={e=>setPassword(e.target.value)} autoFocus required/></div><button className="btn" style={{width:"100%",marginTop:14}} disabled={loading}>{loading?"Entrando...":"Entrar"}</button><p style={{fontSize:12}}>No ambiente local, se nenhuma senha for configurada, a senha temporária é <b>admin123</b>. Troque antes de publicar.</p></form></div>}
